bug:

Prochain en cours :



- roadmap to V1
    - autoreplace in tscn makes insertmod fail
    - after add newGodot class with auto switch: reload panel
    - documentation
    - test real godot4rust project
    - replace sed by node
    - alignment onready
    doc:
        - panel
        - add godot class
        - insert snippet at good place + panel
        - selection reveal
        - insertmod et auto import
        - all
    tests: 
        - panel:
            root node: context (+new,+switch, -onready)
            root node rust: context (-new, +switchn -onready)
            others: context (-new, -nswitch, +onraeady if current doc)




- roadmap to v2
    - onready with non init: necessite parse "fn init" +++
    - code action rename ++
    - ajouter les groups de nodeItem (parse, filter by) +/-
    - parser tscn refactor ? +/-
    - 2 case converts ++


- untestable:
    - insert mod: maybe du to ra loading time